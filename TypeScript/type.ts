// boolean
let isDone: boolean = false;
console.log(isDone);

// number
let decLiteral: number = 6;

// string
let myName: string = "bob";

// array
let list: number[] = [1, 2, 4];

// tuple
let x: [string, number, number, boolean] = ['hello', 1, 2, false];

// enum
enum Color {Red, Green, Blue}
let c: Color = Color.Green;